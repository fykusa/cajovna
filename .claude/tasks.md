# Tasks — Cajovna (čekající)

- [ ] [2026-07-08] **Skutečné překlopení na ostrou produkci (kořen domény)**: `/testovaci/` se prozatím bere jako produkce. Až přijde na řadu opravdový root `taocajovna.cz`: ověřit `.htaccess` (`CGIPassAuth On`, plochá struktura — viz [[production-deploy]]), zopakovat migrace (`db/migration_2026-07-03_kod_polozky.sql`, `db/migration_2026-07-08_nadobi_etnoshop.sql` — DB je sdílená s testovaci, možná už není potřeba, ověřit), upravit `deploy/deploy-testovaci.sh` (nebo vytvořit `deploy-produkce.sh`) pro produkční FTP cestu, nahrát `config.php`/`config/sheets.php` s produkčními hodnotami.

## Hotovo

- [x] [2026-07-17] **Zrušit /pos a /pos-desktop, nechat jen /cajovna**: Smazáno 44 souborů (`pages/MobilePOS.tsx`, `pages/POS.tsx` + testy, celé `components/pos/` a většina `components/pos-mobile/`, `hooks/{usePOS,useMobilePOS,posHelpers}.ts` + testy, e2e `pos-flow.spec.ts`/`mobile-pos-flow.spec.ts`) a routy `/pos`, `/pos-desktop` z `AppRouter.tsx`. Zadání v tasku bylo neúplné — skutečný rozsah zahrnoval ~15 dalších komponent navíc (panely, `SaleDetailView`, mobilní podkroky). Tři soubory sdílené s `/cajovna` zachovány: `MobileHeader.tsx`, `MobileTopBar.tsx` a `MobilePOS.module.css` (přejmenován na `CajovnaPOS.module.css`, import upraven v `CajovnaPOS.tsx`). Kosmetický úklid: `/pos` stub cesty v `Login.test.tsx`/`ProtectedRoute.test.tsx` přejmenovány na `/cajovna`. `tsc --noEmit` čistý, 184/184 testů zelených (pokles ze 258 odpovídá smazaným test souborům). `components/pos/{CategoryList,SearchResults,TeaList}.tsx` identifikovány jako již dřív mrtvý kód, mimo rozsah tohoto tasku, ponechány.
- [x] [2026-07-17] **Potvrzovací/editační dialog ceny před vložením do košíku (/cajovna)**: Nový krok `'confirm'` mezi `quantity` a `checkout` v `useCajovnaPOS.ts` (`CAJE_VIEW_ORDER`). `selectKusu(n)` teď jen uloží `pendingKusu` a přepne view; nová `confirmAddToCart(celkCena)` vloží položku do košíku s upravenou cenou (jednotková `baleni.cena` zůstává katalogová, beze změny). Nová komponenta `CajeConfirmPrice.tsx` (vzor `CajeCheckout.tsx` — editovatelný number input, focus-select, validace ≥0). Zapojeno do `CajovnaPOS.tsx` + `CajeProgressBar`. TDD: 38/38 testů `useCajovnaPOS.test.ts` + 5 nových `CajeConfirmPrice.test.tsx`, celkem 258/258 zelených, `tsc --noEmit` čistý. Editovatelné pole „Zaplaceno" v `CajeCheckout.tsx` zůstává beze změny (schválně, pro dodatečné zaokrouhlení/slevu při platbě). Ruční ověření v prohlížeči nechává uživatel na sobě.
- [x] [2026-07-17] **Sync — case-insensitive kolize KOD**: `assertUniqueKod` v `backend/lib/sheets_sync.php` teď normalizuje `mb_strtoupper` před porovnáním, souhlasí s `uq_kod` ci kolací. Přidán test (case-lišící se duplicita) do `backend/tools/test_sheets_sync.php`, TDD (nejdřív ověřeno FAIL, pak fix → PASS, 24/24 zelených). Spuštěno přes `docker exec cajovna-php-1 php ...`.
- [x] [2026-07-17] **Zkontrolovat Apps Script po změně sync API**: ověřeno — Apps Script (`docs/superpowers/specs/2026-06-08-google-sheets-sync-design.md`) je fire-and-forget (`UrlFetchApp.fetch` s `muteHttpExceptions: true`, tělo odpovědi neparsuje). Změna formátu `synced` na `{synced, vyrazeno}` ho nijak neovlivňuje, žádná úprava potřeba.
- [x] [2026-07-17] **Dead code cleanup**: smazán `backend/index.php` (mrtvý placeholder, deploy skript ho stejně nenahrával) a `getTeas()` wrapper z `frontend/src/api/teas.ts` (nulový počet volajících). Ověřeno `tsc --noEmit` (čisté) a `vitest run` (252/252 zelených).
- [x] [2026-07-07] **Deploy KOD + POS země na testovaci (bráno prozatím jako produkce)**: Migrace `db/migration_2026-07-03_kod_polozky.sql` proběhla (přes potíže s `PREPARE/EXECUTE` v phpMyAdmin a osiřelý index `fk_polozky_kod` po předchozím částečném pokusu — vyřešeno `DROP FOREIGN KEY` + opětovné `ADD CONSTRAINT`). Cestou se objevil a vyřešil samostatný bug: live `.htaccess` na Forpsi (běží jako `cgi-fcgi`) vůbec nepředával `Authorization` hlavičku do PHP → `CGIPassAuth On` přidáno přímo do live souboru (repo `backend/.htaccess` NENÍ to, co běží na serveru, viz [[production-deploy]]). Backend + FE (`npm run build`, testovaci mode) nahráno, sync ze Sheets proběhl, appka funguje.
- [x] [2026-07-09] **Nádobí + Etnoshop nasazeno na testovaci**: Merge do master (`036ace3`), migrace `db/migration_2026-07-08_nadobi_etnoshop.sql` spuštěna přes phpMyAdmin Import, `config/sheets.php` na serveru opraven (správné gid pro nadobi/etnoshop CSV), sync ověřen end-to-end (10+10 položek, správný obsah). Cestou vznikl a vyřešil se závažný deploy incident — viz [[production-deploy]] (plochá vs. `backend/` struktura, nechráněné dev skripty na živém serveru, špatný `config.php`).
- [x] [2026-07-09] **Automatizovaný FTP deploy pro /testovaci**: `deploy/deploy-testovaci.sh` (curl-based, whitelist backend souborů, čištění starých hashovaných assets), credentials v gitignored `deploy/.env.deploy`. Nahrazuje ruční FTP kopírování.
- [x] [2026-07-09] **Bezpečnostní úklid živého /testovaci**: smazány nechráněné dev/CLI skripty přímo dostupné přes URL (`db_reset.php` — TRUNCATE bez auth, `install.php`/`migrate_kasa.php` — fake heslová kontrola, `seed*.php`, `dbtest.php`, `export.php`/`import.php`, celé `tools/`) + redundantní `config_Forpsi.php`/`Dockerfile`. Viz [[production-deploy]] pro detaily.
- [x] [2026-07-09] **Minimální délka hesla snížena na 4 znaky**: požadavek majitele. `backend/api/{auth,users}.php` + `frontend/src/pages/{Login,admin/Users}.tsx`. Nasazeno na testovaci.
- [x] [2026-07-09] **Editovatelná zaplacená částka v CajovnaPOS checkoutu**: `CajeCheckout.tsx` má editovatelné pole "Zaplaceno" (default = dopočtená suma, focus vybere celý obsah, bez spinnerů), posílá se jako `celkem_zaplaceno` a ukládá do `00_prodej.total_kc`. Vedlejší nález a oprava: `CajeHistory.tsx` počítal "dnešek" přes UTC (`toISOString()`) místo lokálního data, kolem půlnoci vypadával čerstvý prodej z přehledu v pokladně — opraveno stejným vzorem jako `periodRange.ts`. Nasazeno na testovaci.
- [x] [2026-07-03] **KOD položky jako business klíč**: Sloupec D sheetu = jednoznačný kód (`YYMM-K-DRUH-ZEME-C`). Sync TRUNCATE→UPSERT podle KOD, `V_SHEETU` flag, `00_prodej_polozky.caje_kod` + FK, nová test data, sloupec KOD v admin/Čaje. Spec: `docs/superpowers/specs/2026-07-03-kod-polozky-design.md`. Merge do masteru 2026-07-07.
- [x] [2026-07-07] **POS — dvoustupňový výběr kategorie → země**: Krok zemí („Vše" + země) jen u kategorií s 2+ zeměmi, oprava ignorované země ve filtru čajů. Spec: `docs/superpowers/specs/2026-07-07-pos-vyber-zeme-design.md`. Commit `a1e7208`.
- [x] [2026-06-17] **Storno prodejů** (#1): Soft-delete (`cancelled_at`/`cancelled_by`) na `00_prodej`. Admin stornuje z Přehledu (detail prodeje + confirm overlay). Prodavačka stornuje vlastní dnešní prodej z CajeHistory. Stornované prodeje se nezapočítají do statistik. 212/212 testů. Commit: `dd81f1a`.
- [x] [2026-06-17] **POS Desktop — přístup na /pos-desktop** (#4): Desktop POS je dostupný přímo přes URL `/pos-desktop`. Prodavačka se po přihlášení routuje na `/cajovna`. Žádná select obrazovka.
- [x] [2026-06-17] **Google Sheets → DB Sync (fáze 2 — sync logika)** (#3): Záložka CAJE v Google Sheets → tabulka `01_caje` v DB. Sync logika implementována, merge do masteru.
- [x] [2026-06-17] **Fáze 4 — Deploy** (#2): Build, upload na Forpsi, .htaccess, import DB, test live. Produkce funkční.
- [x] [2026-06-13] **DB migrace — spustit `01_caje`**: Hotovo lokálně (2026-06-13). Na produkci spustit po deployi.
- [x] [2026-06-05] **POS — 3-panel redesign: Balení → Množství → Pytlík**: Nový krok `configure` nahradil sekvenční `quantity→bag_yn→bag_material→bag_volume`. Tři vertikální panely vedle sebe, ArrowLeft/Right přepíná aktivní panel, ArrowUp/Down naviguje uvnitř. Nová komponenta `ConfigurePanel.tsx`. Odstraněny `QuantityModal`, `QuantitySelector`, `BagSelector`. Helper funkce `getPackagingOptions` + `getBagList` exportovány z `usePOS.ts`. 152/152 testů. Merge `e02e26e`.
- [x] [2026-06-05] **Úprava History panelu — tabulkový přehled**: Přepracován HistoryPanel z jednoduchého seznamu na tabulkový přehled s 6 sloupci. 146/146 testů. Commity: `c7dbb47`–`7da8b85`.
- [x] [2026-06-05] **POS — Scroll do viditelnosti**: Šipky navigaci scrollují aktivní prvek do viditelnosti. `requestAnimationFrame` + `scrollIntoView`. 140/140 testů. Commit: `23c811c`.
- [x] [2026-06-05] **POS — Historia bug fixnuta (Forbidden)**: getSales a getSaleItems vyžadují `requireAuth()` místo `requireAdmin()`. Commity: `21ceab2`, `38bf9d1`.
- [x] [2026-06-05] **POS — Panel s historií dnešních prodejů**: Dvousloupcový layout levého POS panelu, HistoryPanel + SaleDetailView komponenty, keyboard navigace. 140/140 testů.
- [x] [2026-06-05] **POS — Escape zruší rozpracovanou položku**: CANCEL_ITEM reducer akce, Escape před INPUT guardem. 133/133 testů.
- [x] [2026-06-05] **POS — cena pytlíku v košíku**: Sdílený helper `cartTotals.ts`, pytlík jako vlastní řádek v košíku, total zahrnuje obal. 129/129 testů.
- [x] [2026-06-05] **Tržby — přepracování + Dashboard graf úpravy** (merge `1fcf851`): Denní/měsíční pivot, souhrn, HBarChart, fmtKc, periodRange. 127/127 testů.
- [x] [2026-06-05] **Dashboard — graf tržeb v čase** (merge `f096520`): RevenueChart SVG, revenueBuckets, nulové intervaly šedě.
- [x] [2026-06-05] **Dashboard — multi-select filtr kategorií a čajů** (merge `9cd7954`): Set místo jedné hodnoty, backend bere category_ids/tea_ids.
- [x] [2026-06-05] **Fix akce řádku po editaci** (merge `10fac7d`): merge `{ ...row, ...updated }` místo nahrazení.
- [x] [2026-06-05] **Export / Import celé DB** (merge `adbf961`): ZIP export, selektivní import, db_transfer.php, CSV s desetinnou čárkou, iconv autodetekce.
- [x] [2026-06-02] **Edit kategorií/pytlíků + EditableGrid + toasty** (merge `7e810da`): CRUD, FK-guard 409, dvoukrokové mazání, sdílený EditableGrid, toast notifikace. 109/109 testů.
- [x] [2026-05-31] **Admin Layout Redesign**: Horizontální header místo sidebaru. 81/81 testů. Commit: `345d0ed`.
- [x] [2026-05-29] **Fáze 3 — Admin KOMPLETNÍ**: Dashboard, Users, Products, Bags, Sales. 81 testů.
- [x] [2026-05-31] **Admin: Správa uživatelů**: Users.tsx, 5 testů. Commit: `4143b47`.
- [x] [2026-05-29] **Fáze 2 — POS KOMPLETNÍ**: usePOS hook, 7 komponent, keyboard handler, 5 E2E testů.
- [x] [2026-05-31] **Playwright E2E**: 5 testů POS flow. Commit: `f65005d`.
- [x] [2026-05-29] **POS stránka**: POS.tsx, keyboard handler, CheckoutDialog. Commit: `3da9b32`.
- [x] [2026-05-27] **Fáze 0 — Docker prostředí**: docker-compose.yml (PHP 7.4 + MySQL 5.7 + phpMyAdmin).
- [x] [2026-05-27] **DB schema**: schema.sql — users, tea_categories, teas, bags, sales, sale_items.
- [x] [2026-05-27] **PHP backend**: db.php, middleware.php (JWT), auth.php, products.php, bags.php, sales.php, stock.php.
- [x] [2026-05-28] **Seed import**: 294 čajů, 18 pytlíků, 30 kategorií z CSV.
- [x] [2026-05-28] **Fáze 2 — Foundation**: Scaffold, apiFetch, Zustand auth store, Login, ProtectedRoute+AppRouter. 21 testů.
- [x] [2026-05-28] **Task 0 — Vite scaffold + tooling**: Vite + React + TS, Vitest, Zustand, react-router-dom.
