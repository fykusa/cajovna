# Tasks — Cajovna (čekající)

- [ ] [2026-07-03] **KOD položky jako business klíč**: Sloupec D sheetu = jednoznačný kód (`YYMM-K-DRUH-ZEME-C`). Sync TRUNCATE→UPSERT podle KOD, `V_SHEETU` flag, `00_prodej_polozky.caje_kod` + FK, nová test data. Spec: `docs/superpowers/specs/2026-07-03-kod-polozky-design.md`. Další krok: writing-plans.

